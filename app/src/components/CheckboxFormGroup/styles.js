const styles = (theme) => {
  return {
    container: {
      padding: theme.space(2, 'px'),
    },
    formGroup: {
      display: 'flex',
      flexDirection: 'column',
    },
    check: {
      display: 'flex',
      textAlign: 'justify',
      width: '100%',
    },
  }
}

export default styles
