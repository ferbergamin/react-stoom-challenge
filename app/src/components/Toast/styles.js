const styles = (theme) => {
  return {
    toast: {
      position: 'absolute',
      bottom: 0,
      left: 0,
      marginLeft: '10px',
      minWidth: '30%',
    },
    alertBody: {
      marginBottom: ' 0px',
    },
  }
}

export default styles
