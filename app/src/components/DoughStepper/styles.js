const styles = (theme) => {
  return {
    container: {
      padding: theme.space(2, 'px'),
    },
    formGroup: {
      display: 'flex',
      flexDirection: 'column',
    },
    check: {
      display: 'flex',
      flexDirection: 'column',
      textAlign: 'justify',
    },
  }
}

export default styles
