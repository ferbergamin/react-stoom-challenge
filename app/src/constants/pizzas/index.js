import { default as pizzaDoughs } from './pizzaDoughs'
import { default as pizzaSizes } from './pizzaSizes'
import { default as pizzaFillings } from './pizzaFillings'
import { default as tableKeys } from './tableKeys'

const pizza = {
  pizzaDoughs,
  pizzaSizes,
  pizzaFillings,
  tableKeys,
}

export default pizza
